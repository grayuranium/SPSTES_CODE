<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spstes.dao.LoginDao">
	<select id="findUserByAccountAndPwd" resultType="UserInfo">
     	select * from user_info where login_name = #{login_name} and user_paw = #{pwd};
    </select>
    
	<select id="findAdminByAccountAndPwd" resultType="UserInfo">
     	select * from administrator where login_name = #{login_name} and mag_paw = #{pwd};
    </select>
</mapper>